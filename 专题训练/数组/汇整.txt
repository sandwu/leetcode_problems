
其他：
1.在数组中删除给定值的所有值并返回长度，in place操作
2.找出数组中出现次数超一半的数
3.数组中两数之和等于给定值，有且只有一对
4.构造杨辉三角，给定数字n
5.指定的数字旋转数组，要求要原地修改
6.检测数组是否有重复值且相距不超过k
7.求数组中缺失的那个数
8.给一个数组加1求返回的数组

9.从前序遍历和中序遍历 构造二叉树
10.从中序遍历和后续遍历，构造二叉树
11.顺时针旋转有序数组90度
12.求一个数组的最大乘积子数组

定义快慢指针：

1.原地移除数组中为0的到后面
2.有序数组中两数之和等于给定值，仅有一对
3.有序数组中删除重复值并返回长度，不能增、删空间
4.有序数组中找到某个值的下标，如果不存在则返回其应该插入的位置
5.给数组[0,1,2]原地排序
6.求坐标系里的最大面积
7.3Sum(求等于)、4Sum
8.3Sum Closest(求最接近target值)
9.给n+1个数字在长度为n的数组，找出其中的重复值。

卡登+DP：
1.最好的时间买进和卖出货物 1次
2.求数组中里连续最大子数组

贪心：
1.最好的时间买进和卖出货物 多次
2.jump game(给定数组，每个值对应下标是否能到达)


dfs：
1.数组中和为目标值的所有集合1:允许重复利用一个值 (combination sum)
2.数组中和为目标值的所有集合2:不允许重复利用一个值
3.数组中和为目标值的所有集合3:给定范围n和几个数k

二分法：
1.一个有序数组按某个值旋转后，找出一个数的下标
2.一个有序数组，找出等于target的最左和最右坐标
3.一个有序数组按某个值旋转后，找出其内的最小值
4.find peak(找到数组的峰值，要注意nums[-1]=-mix，所以可用二分法)


合并排序：

1.合并有序数组2到有序数组1











